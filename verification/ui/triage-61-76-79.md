# UI Issue Reproduction Notes

Baseline: upstream `a8bd0eb`; local checks performed with a dedicated test vault, independent of the user's actual Obsidian data.

## Issue 76: Settings Toggles

- The active theme already contains `.modal .checkbox-container { width: var(--header-icon-size) !important; height: var(--header-icon-size) !important; }` at lines 40749-40752.
- `git blame` attributes that rule to `bba7b73edce7aeffc930227e6546ae83465c3ac3` (2026-01-24, version 1.11.0).
- The exact Obsidian 1.12.4 app CSS was downloaded from the official `obsidianmd/obsidian-releases` release archive and extracted only into ignored runtime storage.
- Current theme with that exact CSS renders seven standard/grouped/small on/off/disabled fixture controls as 16 x 16 px with the hidden thumb retained. These fixtures are CSS tests, not a running Obsidian 1.12.4 binary.
- Real Obsidian 1.13.7 opens settings in a separate window. The **Quick font size adjustment** control called out by the issue screenshot renders at 16 x 16 px with nontransparent `oklch(0.24 0 0)` background. Screenshot: `76-current-real.png`.
- The reported missing/misaligned control was not reproduced in these environments. Do not submit a duplicate geometry fix without the reporter's exact theme/settings/runtime state. A stale-theme explanation remains unproven.

## Issue 61: iOS Heading Contrast

- Tested current theme with body classes `theme-light is-ios is-mobile is-phone` in Playwright WebKit 18.4 (build 2158) and WebKit 26.6 (build 2359).
- Both engines compute H1 and H4 text as `oklch(0.975 0.03 202.509995)` on the default dark teal background, and dark text at lightness 0.3 for H2/H3/H5/H6. Internal heading links follow the same contrast.
- `61-before-webkit18.png` and `61-before-webkit.png` show those current results. Matching JSON files contain all six heading and link colours.
- A follow-up examines the actual Header settings schema rather than assuming the report's wording names a setting. There is no current header preset called **Base Colours**, and no separate **Responsive Header Text Colours** switch. Standard heading backgrounds explicitly use `--color-accent`, `--color-accent-1`, and `--color-accent-2`; changing a global base-colour preset does not change that mapping. The **Note on Header Text Colours** setting describes automatic contrast, while **Custom Header Colours (Background Colours)** emits `ssopt-headers-custom-colours`.
- The follow-up matrix covers 32 configurations: WebKit 18.4 and 26.6, light and dark mode, and eight cases per pair: standard accents, the global Vivid/Hagia Sophia base preset, Nebula accent preset, default custom HSL backgrounds, custom dark hex colours, base-colour variables as custom heading backgrounds, mixed dark/light custom colours, and explicit manual text colours. All 384 H1-H6 instances pass in both reading markup and editor heading/link class markup. Editor internal links use the `cm-hmd-internal-link > .cm-underline` span structure.
- Exact shared platform classes are `theme-light is-ios is-mobile is-phone` or `theme-dark is-ios is-mobile is-phone`. Global base-preset cases add `ssopt-col-base-lightness-l-vivid ssopt-col-base-tone-l-hagia-sophia` or their `-d-` counterparts. Accent cases add `ssopt-col-accent-l-nebula` or `ssopt-col-accent-d-nebula`.
- Base-colour heading-background cases add `ssopt-headers-custom-colours` and the appropriate Hagia Sophia tone class. In light mode, `--col-accent-custom-headers-0` is `var(--color-base-95)` (dark), `--col-accent-custom-headers-1` is `var(--color-base-05)` (light), and `--col-accent-custom-headers-2` is `var(--color-base-45)`. Dark mode swaps the first two references so H1 remains dark. These variables exercise the theme's custom-background path directly; this does not claim a nonexistent base-colours dropdown option exists in the plugin UI.
- Both engines render the base-colour H1 background at OKLCH lightness 0.01 in light mode and 0.15 in dark mode, with title and reading-link lightness 0.975 and editor-link lightness 0.88. Custom `#112233` backgrounds also produce those bright text/link values. Light custom backgrounds correctly produce dark title/link colours.
- The **Complete Header Colour Control** case adds `ssopt-headers-custom-colours-max` and explicitly sets `--col-custom-headers-H1` through `--col-custom-headers-H6` to `#000000`. Black text is then an intentional manual override in both engines, while internal links retain their automatic contrast. This is a configuration boundary, not evidence of the reported iOS bug or a claim about the reporter's settings.
- Full settings metadata, exact classes/variables, engine versions, and measured colours are in `61-heading-contrast-matrix.json`; the runnable local check is `harness/ui-header-contrast.cjs`. Eight follow-up screenshots use `61-{webkit18,webkit26}-{light,dark}-{custom-base-colours,custom-dark}.png`.
- No iPhone/iPad hardware or native iOS Obsidian instance was available. These Windows WebKit fixtures cannot establish native iOS correctness or rule out an older OS/rendering/configuration issue.
- No code change was made because the report did not reproduce in either tested WebKit generation. The CSS `-infinity` expression alone is not evidence of this bug; both tested engines evaluate it correctly.

## Issue 79: Translucent Window Colours

- The issue screenshot's grey sidebars are consistent with Obsidian's `.is-translucent:not(.is-fullscreen)` CSS path. This is a reproduction hypothesis supported by the cascade, not confirmation of the reporter's setting.
- Exact test classes: `theme-dark mod-macos ssopt-col-base-lightness-d-vivid ssopt-col-base-tone-d-hagia-sophia ssopt-col-accent-d-nebula`, then vary `is-translucent` and `is-fullscreen`.
- In the actual Obsidian 1.13.7 Windows renderer, the opaque window and the fullscreen-class case paint the titlebar and sidebars with `oklch(0.26 0.05 295)`.
- Adding `is-translucent` without fullscreen makes those sidebars transparent via Obsidian's `!important` overrides; titlebar/app-container use `oklch(0 0 none / 0.6)` from the native `--workspace-background-translucent` variable.
- The theme does not define that translucent-background variable. Obsidian intentionally excludes this rendering path in fullscreen, explaining the mode-specific difference.
- Evidence: `79-class-cascade-results.json` and `79-{opaque-window,translucent-window,translucent-fullscreen}-real-renderer.png`.
- These screenshots use a real Windows Obsidian renderer with macOS CSS classes. Native macOS vibrancy/background compositing was not available and is not simulated.
- A bounded fix is available on branch `codex-willemstad-79-translucent-colors`: map the native translucent tint to the selected base palette, or opposite palette for cross-colour UI, while retaining the 60% opacity and transparent child surfaces.
- `79-tint-results.json` records 72 real-renderer before/after cases across light/dark, Hagia Sophia/Tropical Lagoon, normal/inverted/imported cross-colour modes, and opaque/translucent/fullscreen classes. All assert the expected palette tint, unchanged opacity, transparent children, and unchanged opaque/fullscreen backgrounds.
- The new screenshots `79-{before,after}-tint-{light,dark}-real-renderer.png` are renderer-level evidence. Native macOS confirmation remains outstanding; preserving translucency means the final appearance can still differ from opaque/fullscreen rendering according to the user's desktop backdrop.
